const networkMapping = require("./networkMapping.json")
const nftMarketplaceAbi = require("./MP_NFTMarketplace.json")
const nftAuctionAbi = require("./MP_NFTAuction.json")
const nftAbi = require("./BasicNft.json")

module.exports = {
    networkMapping,
    nftMarketplaceAbi,
    nftAuctionAbi,
    nftAbi,
}
